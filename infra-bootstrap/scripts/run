#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [[ "${1:-}" == "legacy" ]]; then
  shift
  exec python3 "$SCRIPT_DIR/infra_bootstrap.py" "$@"
fi

exec python3 "$SCRIPT_DIR/bootstrap_v2.py" "$@"
